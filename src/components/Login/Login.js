import React from 'react';
import useAuth from '../hooks/useAuth';

const Login = () => {

    return (
        <div>
            <h1 className='text-center'>Sign in using a Google Account</h1>
        </div>
    );
};

export default Login;